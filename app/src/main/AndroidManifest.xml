<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".app.SmsMonitorApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmsMonitor"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".app.settings.SettingsActivity"
            android:exported="false"
            android:label="Settings" />

        <activity
            android:name=".app.logs.LogsActivity"
            android:exported="false"
            android:label="Forwarding Logs" />

        <activity
            android:name=".app.keywords.KeywordsActivity"
            android:exported="false"
            android:label="Keywords" />

        <activity
            android:name=".app.logs.LogDetailActivity"
            android:exported="false"
            android:label="Log Detail" />

        <activity
            android:name=".app.testapi.TestApiActivity"
            android:exported="false"
            android:label="Test API"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".app.comparetest.CompareTestActivity"
            android:exported="false"
            android:label="Compare Test"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".app.claim.ClaimCodeScannerActivity"
            android:exported="false"
            android:label="Scan Claim Code"
            android:screenOrientation="portrait" />

        <activity
            android:name=".app.claim.ManualCodeEntryActivity"
            android:exported="false"
            android:label="Enter Claim Code"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".app.claim.TenantSelectionActivity"
            android:exported="false"
            android:label="Select Tenant" />

        <receiver
            android:name=".app.NetworkChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".app.SmsBroadcastReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".app.BootCompletedReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".app.service.ForwardingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".app.service.HeartbeatService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".app.service.DeviceIdentificationService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
